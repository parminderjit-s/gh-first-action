name: First Workflow
on: [workflow_dispatch]
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Greeting
        run: echo "Hello Justin"
      - name: Print Goodbye
        run: echo "Done, thank you"
        
      
